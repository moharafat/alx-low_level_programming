#include "main.h"
/**
 * *leet -  encodes a string into 1337.
 *@s: string parameter
 * Return: string s
 */
char *leet(char *s)
{
char b;
int i;

	for (i = 0; s[i] != '\0'; i++)
    {
        if s[i] = {a,A,e,E,o,O,t,T,l,L}  //only 1 if conditional allowed
		
		s[i] = '4';  //subtraction won't work like in the previous task
    }
}
