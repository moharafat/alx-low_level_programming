#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
int a = 57
int b = 102

while (a >= 48 && a <= 57)
	{
	putchar(c);
	}

while (a >= 97 && a <= 102)
	{
	putchar(c);
	}


	putchar('\n');
return (0);
}
