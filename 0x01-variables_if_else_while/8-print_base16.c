#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
int a = 57
int b = 102

while (a >= 48 && a <= 57)
	{
	putchar(a+ '0');
	}

while (b >= 97 && b <= 102)
	{
	putchar(b+ '0');
	}


	putchar('\n');
return (0);
}
