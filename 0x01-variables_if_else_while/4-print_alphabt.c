#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/

int main(void)
{

	char a;

	for letter (a = 'a'; a <= 'z'; a++)
		{
		if (a != 'q' && letter != 'e')

				putchar(a);

		}


	putchar('\n');

return (0);
}
