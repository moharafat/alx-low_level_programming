#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
int digi = 0;
	while (digi <= 9)
	{
		putchar (digi + 48);

		if (digi != 9)
		{	
			putchar (,);
			putchar ( );
		}
		digi++;

	}
	putchar('\n');
return (0);
}
