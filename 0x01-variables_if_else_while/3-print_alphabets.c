#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: always 0 (Success)
*/
int main(void)
{
	
int a = 97;
int b = 65;

while (a >= 97 && a <= 122)
	{
	putchar(a);
	a++;
	}

while (b >= 65 && b <= 90)
	{
	putchar(b);
	a++;
	}
	putchar("\n")

return (0)
}
