#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void more_numbers(void)

{
	int x, l;

			for (l = 0; l < 10; l++)

			{
				for (x = '0'; x <= '14'; x++)

				_putchar(x);
				_putchar('\x');
			}
}
