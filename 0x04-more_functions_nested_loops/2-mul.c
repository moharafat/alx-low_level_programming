#include "main.h"
/**
 * mul - multiplies two integers
 *@a: first integer to be added
 *@b: second integer to be added
 *
 * Return: multiplication which is the result of multiplication
 */

int mul(int a, int b)

{

	int multiplication = (a * b);

	return (multiplication);
}
