#include "main.h"
/**
 * _atoi -  function that convert a string to an integer
 *@s: string input parameter
 * Return: Always 0.
 */

int _atoi(char *s)
{
	unsigned int num = 0;
	int sign = 1;

	do {
			if (*s == '-')
				sign *= -1;
			else if (*s >=)
		}

}
