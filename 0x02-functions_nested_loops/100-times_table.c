#include "main.h"

/**
 * main - Entry point
 *
 * Description: using sizeof to print the size of the various types.
 *
 * Return: Always 0 (Success)
*/

void print_times_table(int n)

{
	int number

	if (number <= 15) && (number >= 0)

		for (n = 0; number <= n; number++)
		{
			_putchar(48);
			for 


		}



return = (0)

}
