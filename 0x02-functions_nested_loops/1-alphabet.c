#include <stdio.h>
#include "main.h"
nclude <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Return: always 0 (Success)
 *    */
int main(void)
{
	int a;

	for (a = 97;a >= 97 && a <= 122; a++)
			{
					putchar(a);
						
			}
	putchar('\n');
	return (0);
}
