#include <stdio.h>
#include "main.h"

/**
 *  * main - Entry point
 *   *
 *    * Return: always 0 (Success)
 *    */
int main(void)
{
	int a;

	for (a = 97;a >= 97 && a <= 122; a++)
			{
					_putchar(a);
						
			}
	_putchar('\n');
	return (0);
}
